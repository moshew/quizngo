# QuizNGO Admin Environment Configuration

# Load Balancer URL - the admin resolves game PINs via the LB, then connects directly to the game server
# Examples:
# - For localhost only: http://localhost:5000
# - For network access: http://192.168.1.100:5000 (replace with your LB machine's IP)
# - For production: https://your-domain.com

# VITE_LB_URL=http://192.168.1.100:5000
# Leave unset to auto-detect from browser's hostname (recommended)

# To find your local IP address:
# Windows: ipconfig (look for IPv4 Address)
# Mac/Linux: ifconfig or ip addr show
